# Template decorative assets

Template-owned decorative binaries live under their template directory and are resolved by the Web decorative registry. Website data stores semantic intent only; it must never store these paths or manifest IDs.

Canonical future layer order: base, background image, texture, pattern, overlay, content, frame, foreground edge. Decorative render layers must be absolute or background-based, pointer-inert, and contribute zero layout space.
